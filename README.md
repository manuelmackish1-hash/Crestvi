# Crestvista Next.js Starter

This is a simple Next.js (pages router) conversion of the Crestvista Properties static site.

## Quick start
1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Open http://localhost:3000

Deploy to Vercel: just import the repo — Vercel will detect Next.js automatically.
